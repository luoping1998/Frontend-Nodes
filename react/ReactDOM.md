> 在挂载根组件时，通常通过手动调用 React.render(<App />, document.getElementById("root"))，那 render 到底 做了 什🐎 呢？
# ReactDOM
## ReactWork
## ReactRoot
## ReactBatch
## ReactDOM
